parameters:
- name: 'key'
  default: '"$(Agent.OS)"'
  type: string
- name: 'path'
  type: 'string'

steps:
- task: Cache@2
  inputs:
    key: ${{ parameters.key }}
    path: ${{ parameters.path }}
  displayName: Cache artifacts
